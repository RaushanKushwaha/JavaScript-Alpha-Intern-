// Some rules to define a variable
// 1)) - starts with alphabet letters or underscore('_')  or $
// 2)) - We can not start with any digit when defining a varible name.
// 3)) - variable name should not ba a keyword(Predifined word of JavaScript)
// 4)) - symbol is not allowed rather than ('_') or $
// Examples
 
let name;
let $name;
let _name;
let name1;

let 3name; // it is showing error because it is not allowed.
